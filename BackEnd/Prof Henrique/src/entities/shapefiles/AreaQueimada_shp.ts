export class AreaQueimada {
    gid: number;
    dn: number;
    data: string;
  
    constructor(gid: number, dn: number, data: string) {
      this.gid = gid;
      this.dn = dn;
      this.data = data;
    }
  }